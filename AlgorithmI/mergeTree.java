package AlgorithmI;

import javax.swing.tree.TreeNode;

public class mergeTree {
    
    // public static TreeNode mergeTree(TreeNode a, TreeNode b){
        // if(a == null){
        //     return b;
        // }
        // if(b == null){
        //     return a;
        // }
        
        // a.val = a.val + b.val;
        // a.left = mergeTree(a.left, b.left);
        // a.right = mergeTree(a.right, a.right);

        // return a;
    // }
 }
